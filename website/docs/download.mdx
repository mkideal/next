import FeaturedDownloads from '@site/src/components/FeaturedDownloads';
import useDocusaurusContext from "@docusaurus/useDocusaurusContext";

export const repo = () => useDocusaurusContext().siteConfig.customFields.repo;

# Download

## Latest Version

<FeaturedDownloads />

The latest all releases of Next can be found on the <a target="_blank" href={`${repo()}/releases/latest`}>GitHub release page</a>.

## Old Versions

Older versions are available in our historical releases on <a target="_blank" href={`${repo()}/releases`}>GitHub</a>.

## Source Code

Next source code is hosted on <a target="_blank" href={`${repo()}`}>GitHub</a>.
